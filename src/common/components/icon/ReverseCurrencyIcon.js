import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';

const ReverseCurrencyIcon = ({color = '#fff'}) => {
  return <Icon name={'wifi-protected-setup'} color={color} size={25} />;
};

export default ReverseCurrencyIcon;
